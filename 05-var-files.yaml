- name: variables from files
  hosts: local
  connection: local
  vars_files:
  - course.yml
  tasks:
  - name: print the course info
    ansible.builtin.debug:
      msg: "Course name is:{{course}},and Duration was {{Duration}},and tranier is {{tranier}}"
